<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:orientation="vertical"
    android:padding="16dp"
    android:layout_width="match_parent"
    android:layout_height="match_parent">

    <Spinner android:id="@+id/spinnerWeather"/>
    <EditText android:id="@+id/inputTemp" android:hint="Température" android:inputType="numberDecimal"/>
    <Switch android:id="@+id/switchOutside" android:text="Extérieur ?"/>
    <Button android:id="@+id/btnValidate" android:text="Valider"/>

    <androidx.recyclerview.widget.RecyclerView
        android:id="@+id/recyclerFilteredActivities"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"/>
</LinearLayout>
